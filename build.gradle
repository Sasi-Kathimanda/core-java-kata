apply plugin: 'java'

repositories {
    jcenter()
    maven {
        url 'https://repo.maven.apache.org/maven2'
        name 'Maven Central'
    }
}

dependencies {
    compile 'org.slf4j:slf4j-api:1.7.32'
    compile 'org.junit.jupiter:junit-jupiter:5.8.1'
}

test{
    useJUnitPlatform()
}